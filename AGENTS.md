# AGENTS.md

本文件是 AI Agent 在 LocalLens 仓库中工作的强制入口。LocalLens 是本地优先的图片与视频管理系统，正式架构为 Tauri 2 + React + TypeScript、Rust + Axum + SQLx + SQLite，以及 Kotlin + Jetpack Compose Android 客户端。媒体原文件由用户管理，LocalLens 只维护索引、缩略图、元数据、任务和播放记录。

## 1. 修改前必读

按顺序读取：

1. 当前 Issue、PR 描述、验收标准和明确非目标；
2. `README.md`；
3. `docs/architecture.md`、`docs/api.md`、迁移和安全相关文档；
4. 本文件；
5. 相关 Rust crate、Tauri、Android、数据库迁移和测试。

修改前先确认媒体文件、SQLite、缩略图、FFmpeg、局域网 API、设备配对和平台打包的影响。

## 2. 核心不变量

- 媒体原文件保存在用户选择的目录中，不因收藏、评分、标签、相册或播放记录而被修改；
- 后端只能访问配置中的媒体根目录；
- 绝对路径、`..`、Windows 路径前缀和路径穿越必须被拒绝；
- SQLite 是索引与状态事实来源，旧数据库升级必须先备份并保持可恢复；
- 客户端只接收媒体 ID 和受控相对路径，不暴露任意本地路径；
- 设备 Token 不能获得管理员扫描、配对或撤销其他设备的权限；
- 当前不提供远程删除、移动或重命名原始文件的接口；
- 正式开发树不重新引入已移除的 Go、Flutter 或 React Native 重复实现。

## 3. 风险等级

- **P0**：原文件损坏或删除、数据库不可恢复、任意文件读取、设备认证绕过；
- **P1**：SQLite 迁移、扫描与监听、缩略图/转码队列、配对和 Token、媒体流与 Range、发布打包；
- **P2**：跨端功能、API 兼容、性能、缓存、Windows 或 Android 兼容改造；
- **P3**：低风险 UI、文档、测试和内部整理。

P0/P1 必须有关联 Issue、失败路径测试、独立审查和可执行回滚。

## 4. 强制流程

### 调查

先只读并确认：

- Rust Core、Axum API、Tauri 与 Android 调用链；
- SQLite 表、迁移、WAL/SHM 和备份影响；
- 文件扫描、监听、防抖、缩略图和转码队列影响；
- 媒体根目录、路径校验、Token 和权限边界；
- 已确认事实、推断、未知项与最小修改范围。

### 实现

- 一个 PR 只解决一个主要问题；
- 不混合功能、无关重构、依赖升级和全局格式化；
- 优先复用现有 crate、服务、API、任务队列和平台适配；
- 不因局部需求建立第二套数据库、任务系统、媒体索引或认证机制；
- 不为未确认的未来需求提前抽象；
- 提交说明使用中文；
- 不直接推送 `main`，默认 squash merge。

### 测试先行

以下改动优先先写失败测试：

- SQLite 升级、备份、事务和异常恢复；
- 扫描、文件监听、防抖、大文件稳定性和重复索引；
- 路径穿越、媒体根目录和相对路径解析；
- 配对过期、一次性密钥、Token 哈希、撤销和权限隔离；
- HTTP Range、字幕、直接播放、HLS 转码和缓存清理；
- 任务并发、取消、失败重试、配额和临时文件；
- Android 与桌面端 API 兼容。

不得用 Mock 或内存数据库结果替代 SQLite、真实媒体文件、FFmpeg 或平台行为验证。

## 5. 数据库与文件系统

- 修改数据库结构必须提供前向迁移和旧数据兼容测试；
- 已发布迁移不得直接改写，错误通过新的修复迁移处理；
- 迁移前备份必须包含数据库、WAL 和 SHM（存在时）；
- 大规模扫描不得一次性加载全部媒体到内存；
- 所有队列、缓存、缩略图和转码产物必须有数量、空间或时间边界；
- 删除索引或缓存不得误删原始媒体；
- 配置、测试和日志不得包含真实私人媒体路径、设备 Token 或敏感元数据。

## 6. 网络与权限

- 默认只面向可信局域网，不直接把服务端口暴露到公网；
- 远程访问优先使用可信 VPN；
- `public_url` 必须是受控 HTTP/HTTPS 根地址，不含路径、查询、账号或密码；
- 管理员接口与设备接口保持权限隔离；
- Token 只能以安全哈希形式持久化；
- 错误响应不得暴露本地绝对路径、Token、命令行或底层敏感异常。

## 7. 平台与发布

- Windows 正式构建必须验证 Tauri、Rust 服务、FFmpeg/FFprobe 和安装包；
- Android 构建必须验证 Kotlin、Compose、Media3、网络权限和 APK；
- FFmpeg 是独立第三方程序，发布需保留来源和许可证说明；
- Android 开发签名和 Windows 未签名安装包不得表述为正式商店级发布；
- 新平台不在当前范围内时，不得顺带引入。

## 8. 完成验证

完成前必须提供实际证据：

- Rust `fmt`、Clippy、Workspace 测试；
- Tauri React、Rust 宿主和安装包构建；
- Android APK 构建；
- 真实临时媒体库扫描、缩略图或转码验证（适用时）；
- 旧 SQLite 原地升级与备份恢复；
- 路径穿越、权限拒绝和 Token 撤销；
- 未验证的平台行为和剩余风险。

CI 通过不能替代真实 Windows、Android、FFmpeg、文件系统或局域网验证。