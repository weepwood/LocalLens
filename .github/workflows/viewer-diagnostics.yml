name: Viewer Diagnostics

on:
  pull_request:
    branches: [main]
  push:
    branches: ["feat/immersive-media-viewer-v0.4"]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  flutter-diagnostics:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/local_lens
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.41.9"
          channel: stable
          cache: true

      - name: Generate Android platform project
        shell: bash
        run: |
          flutter create \
            --project-name local_lens \
            --org com.weepwood \
            --platforms android \
            .
          flutter pub get

      - name: Test and analyze
        id: diagnostics
        continue-on-error: true
        shell: bash
        run: |
          set +e
          flutter test > viewer-diagnostics.txt 2>&1
          TEST_STATUS=$?
          echo "" >> viewer-diagnostics.txt
          echo "===== FLUTTER ANALYZE =====" >> viewer-diagnostics.txt
          flutter analyze >> viewer-diagnostics.txt 2>&1
          ANALYZE_STATUS=$?
          echo "" >> viewer-diagnostics.txt
          echo "test_status=$TEST_STATUS" >> viewer-diagnostics.txt
          echo "analyze_status=$ANALYZE_STATUS" >> viewer-diagnostics.txt
          cat viewer-diagnostics.txt
          exit $(( TEST_STATUS != 0 || ANALYZE_STATUS != 0 ))

      - name: Upload diagnostics
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: viewer-diagnostics
          path: apps/local_lens/viewer-diagnostics.txt
          if-no-files-found: error
          retention-days: 7
